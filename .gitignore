.DS_Store
**/.DS_Store

# local tooling / not part of the site
.claude/
graphify-out/

# original certificate folder (root only) — the site serves assets/certificates/
/certificates/

# unused 1.2 MB source for profile.jpg
assets/sufyan image.png

# CV working files: contain personal data and are not linked from the site
/cv/

# notebook source repo (has its own .git; would become a broken gitlink)
/Used Car Price Prediction/
/Medical Insurance cost Prediction/
/House Price Prediction/

# personal German trainer source: .venv, __pycache__, and a vocab.db
# holding review history. german/ is the built static copy that ships.
/German Language/
